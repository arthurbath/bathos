## 1. Native Refresh Command

- [x] 1.1 Recognize and consume Command+Option+R only while the macOS Tasks window is active
- [x] 1.2 Clear only reload-safe WebKit and URL response caches before reloading the current Tasks route from its origin

## 2. Verification and Documentation

- [x] 2.1 Add native tests for shortcut ownership and the protected-storage cache boundary
- [x] 2.2 Document the native shortcut and verify the macOS companion, shared iOS build, and OpenSpec change
