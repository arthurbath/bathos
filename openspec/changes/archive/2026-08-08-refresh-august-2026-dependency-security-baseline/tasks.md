## 1. Baseline And Target Evidence

- [x] 1.1 Record the current root and spike dependency graphs, advisory identities and paths, official fixed versions, Supabase changelog constraints, Node support policy, and the exact bounded target set.
- [x] 1.2 Add a dated evaluation that will retain before-and-after advisory reachability, compatibility decisions, regression evidence, performance comparison, and rollback guidance.

## 2. Node And Npm Dependency Refresh

- [x] 2.1 Declare Node 22 or newer in the root manifest and add a Node 24 LTS local runtime pin.
- [x] 2.2 Update root Supabase JS and Auth to exact 2.112.2, PostCSS to 8.5.26, and select patched Nano ID 3.3.18 and js-yaml 4.3.1 through compatible lockfile resolution without forced remediation.
- [x] 2.3 Align the isolated PowerSync spike to exact Supabase JS 2.112.2 and select patched PostCSS and Nano ID transitive versions without changing PowerSync, WA-SQLite, React, or Vite major lines.

## 3. Edge Dependency Alignment

- [x] 3.1 Align all function-local Supabase maps, the MCP generator input and generated source, and exact-version contract tests to Supabase JS 2.112.2.
- [x] 3.2 Regenerate the reminder and widget Deno locks, retain unrelated exact direct dependencies, and prove a second generation is deterministic.

## 4. Focused Security And Dependency Verification

- [x] 4.1 Prove clean root and spike installations, valid dependency trees, registry integrity, and fresh audits; confirm the js-yaml and Nano ID advisories no longer reproduce and classify every residual finding.
- [x] 4.2 Run focused Supabase browser lifecycle, auth-lock, request retry, dependency-pin, CSS, MCP, Edge type, bundle, and all-function HTTP startup checks without deployment.

## 5. Regression And Performance Verification

- [x] 5.1 Run the complete Vitest suite, Tasks typecheck, lint, development and production builds, and representative browser route smoke checks.
- [x] 5.2 Run offline persistence, multi-client convergence, preservation/recovery, and sustained synchronization integrations against the restored local Supabase stack, preserving local and production data boundaries.
- [x] 5.3 Run applicable native contract and Swift tests plus the complete Tasks performance suite, and compare repeated final averages with the August 4 dependency baseline under the authorized performance waiver.
- [x] 5.4 Complete the dated evaluation, run strict OpenSpec and diff validation, sync the modified durable specs, and archive the completed change.
