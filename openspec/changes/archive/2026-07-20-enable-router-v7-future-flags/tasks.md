## 1. Router Compatibility

- [x] 1.1 Configure the shared `BrowserRouter` with `v7_startTransition` and `v7_relativeSplatPath`.
- [x] 1.2 Add a focused regression test that requires both compatibility flags at the root router boundary.

## 2. Validation and Closeout

- [x] 2.1 Run focused routing tests and verify a live browser load emits neither compatibility warning.
- [x] 2.2 Run lint, build, the full test suite, and strict OpenSpec validation.
- [x] 2.3 Sync the new capability into durable specs, archive the completed change, and commit the verified result without pushing.
