## 1. Native Interaction

- [x] 1.1 Add a macOS Tasks web-view host that accepts AppKit's first mouse event.
- [x] 1.2 Construct the accepting host from the existing SwiftUI representable without changing WebKit delegates or web behavior.

## 2. Verification

- [x] 2.1 Add native regression coverage for first-mouse acceptance.
- [x] 2.2 Run the focused macOS tests and unsigned validation build.
- [x] 2.3 Validate the OpenSpec change strictly and document inactive-window click and drag verification.
