## 1. Shared Date Field

- [x] 1.1 Remove the shared hover text-color override while preserving the neutral date-field background behavior.
- [x] 1.2 Add a regression assertion for stable empty-placeholder styling and unchanged populated-value styling.

## 2. Verification

- [x] 2.1 Run the shared date-picker tests, lint, build, strict OpenSpec validation, and whitespace checks.
- [x] 2.2 Verify an empty Tasks date field retains its computed text color during pointer hover in the rendered app.

## 3. Completion

- [x] 3.1 Sync the shared form-control requirement and archive the completed change.
