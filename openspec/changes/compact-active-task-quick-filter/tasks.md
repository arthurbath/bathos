## 1. Compact Filter Presentation

- [x] 1.1 Group the list title with an active-filter subtitle that appears only for non-All filters
- [x] 1.2 Keep the Quick Filters trigger icon-sized and apply the established Select Tasks active treatment without changing its menu behavior

## 2. Verification

- [x] 2.1 Add Tasks shell regression coverage for the subtitle, stable icon-only trigger, active styling, clearing, and cross-list persistence
- [x] 2.2 Run focused Tasks tests, lint/build validation, and OpenSpec validation
- [x] 2.3 Verify the filtered toolbar and heading at mobile and wider viewport sizes in the running app

## 3. Actionability Multi-select

- [x] 3.1 Define the seven compatible non-empty actionability combinations and their concise subtitles
- [x] 3.2 Replace the radio presets with checked Ready, Rechecking, and Waiting menu items, keeping the menu open across toggles and resetting an empty selection to all three
- [x] 3.3 Expand the durable preference constraint and background widget projection authority without rewriting existing values
- [x] 3.4 Add domain, preference, shell, native widget, and database regression coverage
- [x] 3.5 Run focused and full validation, then visually verify and restore the running app's original preference

## 4. Temporarily Revealed Task Focus

- [x] 4.1 Preserve Quick Find access to tasks excluded by the active quick filter
- [x] 4.2 Abandon keyboard focus when closing a temporarily revealed task hides it again
- [x] 4.3 Add regression coverage and verify the complete interaction in the running app
