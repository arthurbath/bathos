## 1. Shared date-picker formatting

- [x] 1.1 Add a regression test for the shared `YYYY Mon D` selected-date display.
- [x] 1.2 Change the shared date-picker default and apply its formatter to specialized picker triggers.
- [x] 1.3 Replace the remaining ordinary native date input with the shared BathOS date picker.

## 2. Tasks date-picker formatting

- [x] 2.1 Update the specialized Task date-control formatter to use `YYYY Mon D` for explicit dates.
- [x] 2.2 Preserve and test Yesterday, Today, Tomorrow, Someday, and Today-horizon semantic labels.

## 3. Validation

- [x] 3.1 Run focused date-picker and Task date-format tests.
- [x] 3.2 Run lint, build, OpenSpec validation, and diff checks.
- [x] 3.3 Verify representative Tasks and non-Tasks date-picker inputs in the rendered application.
