## 1. Task completion color

- [x] 1.1 Apply semantic success green to every checked to-do state while preserving neutral unchecked, canceled, deleted, hover, and checklist states
- [x] 1.2 Add focused regression coverage for pointer completion, keyboard-command completion, and persisted Done tasks

## 2. Validation

- [x] 2.1 Run focused tests, Tasks typechecking, lint, build, and strict OpenSpec validation
- [x] 2.2 Verify the completed checkbox color and unchanged interaction semantics in the rendered Tasks app
