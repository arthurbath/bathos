# yaml-language-server: $schema=https://unpkg.com/@powersync/service-sync-rules@1.23.3/schema/sync_rules.json

config:
  edition: 3

streams:
  owner_tasks:
    auto_subscribe: true
    queries:
      - SELECT * FROM tasks_spike_items WHERE owner_id = auth.user_id()
